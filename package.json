{
  "name": "mod-stats",
  "version": "0.1.0",
  "description": "Devvit app — tracks moderator actions and publishes monthly wiki reports",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "npx devvit playtest",
    "upload": "npx devvit upload"
  },
  "devDependencies": {
    "@devvit/public-api": "^0.11.0",
    "@devvit/protos": "^0.11.0",
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
